version: 0.2

phases:
  install:
    commands:
      - echo "Install phase - no dependencies needed"
  build:
    commands:
      - echo "Build started on `date`"
      - ls -R
artifacts:
  files:
    - '**/*'
